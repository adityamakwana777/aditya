import PageCard from '../../components/ui/PageCard'

const SettingsPage = () => {
  return (
    <PageCard
      title="Settings"
      description="Adjust preferences and configure automation defaults."
    >
      <div className="placeholder-grid">
        <div className="placeholder-panel" />
      </div>
    </PageCard>
  )
}

export default SettingsPage
